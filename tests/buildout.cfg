[buildout]
parts = django-1.1
develop = ..
eggs = 
  django-piston3
  pyyaml

[django-1.1]
recipe = djangorecipe
project = test_project
settings = settings
test = testapp
eggs = ${buildout:eggs}
testrunner = test-1.1
