[buildout]
parts = django
develop = ..
eggs = 
  django-piston3
  pyyaml

[django]
recipe = djangorecipe
project = test_project
settings = settings
test = testapp
eggs = ${buildout:eggs}
testrunner = test
